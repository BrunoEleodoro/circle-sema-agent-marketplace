import { tools } from './tools';

/**
 * Autonomous Payment Agent built on LangChain Deep Agents.
 * TODO: construct via `createDeepAgent({ ... })` from the `deepagents` package.
 */
export function buildAgent(): unknown {
  void tools;
  throw new Error('buildAgent not implemented yet');
}
