import { tools } from './tools';

/**
 * Autonomous Payment Agent built on the Claude Agent SDK.
 * TODO: construct using `@anthropic-ai/claude-agent-sdk` (query loop + tools + skills).
 */
export function buildAgent(): unknown {
  void tools;
  throw new Error('buildAgent not implemented yet');
}
